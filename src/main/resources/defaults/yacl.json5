{
	// Show the flashing colour picker hint (auto disables after first use)
	showColorPickerIndicator: true,
	// Load .webp and .gif during Minecraft resource reload instead of on-demand
	preloadComplexImageFormats: false
}

